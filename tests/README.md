# Tests (`tests/`)

This folder contains automated tests (pytest).

## Purpose
- Ensure code quality and prevent regressions
- Support CI/CD (GitHub Actions runs tests on PRs)

## How to run locally
From repository root:

```bash
pytest
```

## Notes

For Milestone 1, tests are minimal (smoke tests).
More meaningful tests will be added as the pipeline grows.
